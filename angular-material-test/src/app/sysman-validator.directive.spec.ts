import { SysmanValidatorDirective } from './sysman-validator.directive';

describe('SysmanValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new SysmanValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
