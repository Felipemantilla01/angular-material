import { SysmanFormatterDirective } from './sysman-formatter.directive';

describe('SysmanFormatterDirective', () => {
  it('should create an instance', () => {
    const directive = new SysmanFormatterDirective(el);
    expect(directive).toBeTruthy();
  });
});
